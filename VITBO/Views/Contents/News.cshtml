@model List<VITBO.Models.ContentDto>

@{
    ViewData["Title"] = "News Management";
    int currentLangId = ViewData["CurrentLangId"] != null ? (int)ViewData["CurrentLangId"]! : 1;
}

<div class="card border-0 shadow-sm border-start border-danger border-4">
    <div class="card-header bg-white border-bottom py-3 d-flex justify-content-between align-items-center">
        <h5 class="mb-0 text-danger"><i class="bi bi-newspaper me-2"></i> News Directory</h5>
        <a asp-action="CreateNews" class="btn btn-danger btn-sm"><i class="bi bi-plus-circle me-1"></i> New News Entry</a>

        <form method="get" asp-action="News" class="d-flex align-items-center">
            <label class="me-2 text-muted small fw-bold">Language ID:</label>
            <select name="langId" class="form-select form-select-sm" onchange="this.form.submit()">
                <!option value="1" @(currentLangId == 1 ? "selected" : "")>English (1)</!option>
                <!option value="2" @(currentLangId == 2 ? "selected" : "")>Italiano (2)</!option>
@*                 <!option value="3" @(currentLangId == 3 ? "selected" : "")>French (3)</!option>
                <!option value="4" @(currentLangId == 4 ? "selected" : "")>German (4)</!option> *@
            </select>
        </form>
    </div>
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover table-striped mb-0 align-middle">
                <thead class="table-light">
                    <tr>
                        <th scope="col" class="ps-4" style="width: 5%">ID</th>
                        <th scope="col" style="width: 30%">Headline</th>
                        <th scope="col" style="width: 35%">Preview</th>
                        <th scope="col" style="width: 10%">Status</th>
                        <th scope="col" class="text-end pe-4" style="width: 20%">Published</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        @foreach (var newsItem in Model)
                        {
                            <tr>
                                <td class="ps-4 text-muted fw-bold">#@newsItem.Id</td>
                                <td>
                                    <span class="d-block fw-bold text-dark">@newsItem.Title</span>
                                    @if (!string.IsNullOrEmpty(newsItem.Subtitle))
                                    {
                                        <small class="text-muted d-block text-truncate">@newsItem.Subtitle</small>
                                    }
                                </td>
                                <td>
                                    <span class="text-muted small text-truncate d-inline-block" style="max-width: 300px;">
                                        @(string.IsNullOrEmpty(newsItem.Preview) ? "No preview available" : newsItem.Preview)
                                    </span>
                                </td>
                                <td>
                                    @if (newsItem.IsPublished)
                                    {
                                        <span class="badge bg-success">Live</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary">Offline</span>
                                    }
                                </td>
                                <td class="text-end pe-4 text-muted small">
                                    <i class="bi bi-clock me-1"></i> @newsItem.PublishDate.ToString("g")
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5" class="text-center py-4 text-muted">No news entries found for this language.</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
